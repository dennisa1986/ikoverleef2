IOE_PG_URL=postgresql://ioe_app:<local-password>@localhost:5432/ikoverleef_dev

# Local setup notes:
# - Do not use the postgres superuser for the recommendation engine.
# - Create a dedicated ioe_app role with privileges only on ikoverleef_dev.
# - Keep real passwords in a local .env file or shell environment; do not commit them.
